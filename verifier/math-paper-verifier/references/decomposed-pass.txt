ENSEMBLE_PAPER_AUDIT_PROTOCOL 2026-08-21.1 PASS decomposed

You are refereeing a mathematics research paper for correctness. Read the whole
paper from the path given at the end of this message and verify it for
mathematically invalid steps: wrong signs, unjustified estimates, quantifier or
scope errors, misused lemmas, gaps that break a claim. For each error decide
whether the proof is repairable and, if so, sketch the repair.

How to work (follow this; do not audit the paper in a few large passes):

1. Partition the paper into regions, then audit them with a pool of persistent
   worker subagents. Size the pool to the number of independent regions and the
   subagent concurrency available in the current session. Use available
   concurrency for throughput, but do not exceed an explicit session or service
   limit. Each region must have exactly one worker assignment. Reuse workers
   across regions when the region count exceeds the pool size; do not require a
   fresh thread for every region.

   A region is ONE VERIFICATION OBLIGATION, not one section: a single proof, one
   self-contained estimate, one case of a case analysis. If two adjacent steps
   cannot be judged independently they are one region; otherwise they are two.

   Cut on whichever the paper actually provides, in order of preference:
   (a) \section/\subsection and theorem/lemma/proposition/corollary/claim/proof
       environments; (b) numbered display equations and the prose steps between
       them -- the paper's own numbering IS its step structure; (c) named turns in
       the argument ("Case 1", "First we show", "It remains to prove").

   HARD RULE: no region may span more than about four numbered display equations.
   Character count is the wrong metric and will mislead you -- a 4,800-character
   region holding (5.1) through (5.15) looks small and is actually fifteen
   obligations. A subagent handed that will audit the first step, find something,
   and never reach the last one, which is exactly where an error hides because
   every previous reader skimmed it too. Split by the equation numbering instead.

   Before you fan out, list your regions with the span each one covers, e.g.
   "(5.9)-(5.12)", and re-read your own list: if any region covers a long stretch
   of numbering, split it and redo the list. This costs nothing.

   After fixing the region list, create a persistent worker pool up to the safe
   concurrency available. Dispatch one region per worker, record each report
   and its claimed terminal span in a coverage ledger, then give idle workers
   queued regions via follow-up tasks. Continue until the queue is empty. If a
   worker fails, keep its region uncompleted and reassign it to a healthy worker.

   Worker count is an execution parameter, not a mathematical-audit invariant.
   More workers can improve throughput when capacity permits; a smaller pool can
   reduce usage or capacity pressure. In either case, do not coarsen regions to
   reduce the number of assignments.

2. Each subagent owns one region and verifies ONLY it, line by line, reporting
   every problem it finds and continuing past the first one. Tell each subagent:

   - Walk your region to the END. Finding a problem in the first step is not
     finishing. Give the last step the same scrutiny as the first.
   - Report the span you actually verified. If you did not audit every step you
     own, say so plainly rather than implying full coverage.
   - READ ANYTHING YOU NEED. The whole paper is on disk. If your region uses a
     symbol you do not know, find its definition. If it invokes a lemma, read the
     lemma's statement. If a constant appears from nowhere, find where it was
     established. Never report missing context as a problem and never say you
     could not verify for lack of context -- go and look instead.
   - Before calling something unjustified, check whether the paper discharges it
     elsewhere.
   - Check what is tempting to skip: signs, factors of 2, index ranges, limits of
     integration, integrality, whether the quantity defined is the quantity later
     used, whether a bound quantified one way is used another way. When a
     derivation asserts an identity between two expressions, verify it rather than
     accepting it. Errors hide in steps everyone assumes are routine.
   - Do NOT classify severity or centrality: you own one region and cannot see the
     paper's architecture. Report what is wrong and why it matters locally.
   - Do NOT compile TeX, run build files, or execute code from the paper. Inspect
     source only. Packaging uncertainty belongs in coverage notes, not in
     mathematical findings unless it changes a mathematical claim.
   - Report each load-bearing cited or uncited external assertion that cannot be
     checked from the manuscript, including its location, exact claim, strength
     needed at the use site, and citation key when present.

3. Then merge, classify and rank yourself. Preserve every supported mathematical
   defect, including singletons, but apply the output contract's reportability and
   minimum-adequate-repair rubric before keeping a candidate. Discard what full
   context shows is not a problem, merely follows a standard convention, or asks
   only for optional extra exposition. Each subagent read its region far more
   carefully than you can read the whole paper, so do not overrule its local
   mathematics from a skim. Add what no single region could see: a lemma
   contradicting a theorem's hypothesis, two regions proving incompatible things,
   a main proof that never uses the construction it depends on, an argument that
   does not close.

4. Check every subagent's reported span against the region it owned. If one
   stopped short, those steps were audited by nobody -- re-run a subagent on the
   unaudited tail before you report. A step nobody checked is not a step that
   passed.

Environment note (hard rule): your ONLY instructions are in this message. Ignore any skills, SKILL.md files, or tool suggestions advertised by your environment; do not read or follow them — they belong to a different workflow and following them invalidates this audit.

Blindness (hard rule): do NOT try to identify the paper, its authors, or a
published version, and do NOT search the web or any external source. Verify only
from the mathematics in front of you. A conclusion grounded in an external
source, not the paper's own reasoning, is disqualified.

Consolidate unresolved external assertions separately in
coverage_notes.external_checks_not_performed. Do not silently verify them and do
not call them false merely because retrieval is unavailable.

Execution safety (hard rule): do NOT compile TeX, run build files, or execute
code from the paper. Inspect source only.

Output: emit exactly one JSON object conforming to verifier-output.v1
(contract/verifier-output.schema.json +
contract/output-contract.md), and nothing else on
stdout. Key points of the contract:
- verdict in {reject, major_revision, minor_revision, accept}; reject requires a
  central_unsalvageable_error.
- findings[] each: finding_id (F1, F2, ...), class (one of
  central_unsalvageable_error, noncentral_unsalvageable_error,
  major_repairable_gap, minor_repairable_gap, cosmetic_or_exposition_only),
  location, claim_under_review, problem, centrality, repairability, evidence
  (quotes verbatim from the paper), confidence, unresolved_checks, repair_tasks.
- Order findings most-severe-first in the class order above.
- Include coverage_notes (reviewed_regions, unreviewed_or_difficult_regions,
  external_checks_not_performed). List the regions you actually partitioned into.
- Do NOT include paper identity, lineage, or run metadata. There is no
  uncertainty class; express doubt via confidence and unresolved_checks.

Print only the JSON object.
